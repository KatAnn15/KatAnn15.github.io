export interface SliderChartProps{
    currentPlan: {},
    feature: {
        maxValue: number,
        sliderColor: string,
        value: string
    },
    sliderWidth: number,
    mode: ("full" | "short")
}